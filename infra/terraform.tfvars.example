# Copy to terraform.tfvars and fill in real values
# Never commit terraform.tfvars (contains account IDs / bucket names)

aws_region       = "us-east-2"
environment      = "conference"
app_name         = "btl-transport"
domain_name      = "transport.btl2026.com"      # REPLACE with your domain
ecr_image_tag    = "latest"
desired_count    = 1
cpu              = 512
memory           = 1024
tf_state_bucket  = "btl-transport-terraform-state"
github_org_repo  = "your-org/btl-transport"      # REPLACE
alert_email      = "oluwajobaadegboye@hotmail.com"      # REPLACE — receives CloudWatch alarm notifications
min_capacity     = 1
max_capacity     = 2
